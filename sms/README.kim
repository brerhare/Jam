Client
	install smstools (apt-get)
	edit settings in getFromServer.php
	run as root so it can write to /var/spool/sms/outgoing (in the future exec from a c program to handle setuid properly)

server
	copy files into a location
	create subdir 'data' with write perms

