#css.display = none 
css.display = block

css.overflow = hidden /* (or any type of overflow) avoids position shift on no-margin containers */
css.margin = 0px auto

stacking = vertical
css.border = 1px solid black

# Limitations
# -----------
# Table.field refs on RHS of the '=' MUST be at the end of a line, cannot be embedded anywhere else

