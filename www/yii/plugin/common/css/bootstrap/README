css 'themes' here should generally be named in the format: url.whereifound.it_theme.css

Put the following in your layout

<?php
    $baseUrl = Yii::app()->baseUrl;
    $cs = Yii::app()->getClientScript();
    //$cs->registerScriptFile($baseUrl.'/js/yourscript.js');
    $cs->registerCssFile($baseUrl.'/css/bootstrap/yourbadtaste.css');
?>
